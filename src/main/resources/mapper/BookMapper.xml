<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lpssfxy.ldy.crawlernoveladmin.mapper.BookMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.lpssfxy.ldy.crawlernoveladmin.entity.BookBean">
        <id column="id" property="id" />
        <result column="catId" property="catId" />
        <result column="pic_url" property="picUrl" />
        <result column="book_name" property="bookName" />
        <result column="author" property="author" />
        <result column="book_desc" property="bookDesc" />
        <result column="score" property="score" />
        <result column="book_status" property="bookStatus" />
        <result column="visit_count" property="visitCount" />
        <result column="update_time" property="updateTime" />
        <result column="soft_cat" property="softCat" />
        <result column="soft_tag" property="softTag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, catId, pic_url, book_name, author, book_desc, score, book_status, visit_count, update_time, soft_cat, soft_tag
    </sql>

</mapper>
